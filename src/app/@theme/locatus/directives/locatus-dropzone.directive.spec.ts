import { LocatusDropzoneDirective } from './locatus-dropzone.directive';

describe('LocatusDropzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new LocatusDropzoneDirective();
    expect(directive).toBeTruthy();
  });
});
