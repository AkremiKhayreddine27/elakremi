import { LocatusCurrencyPipe } from './locatus-currency.pipe';

describe('LocatusCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new LocatusCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
